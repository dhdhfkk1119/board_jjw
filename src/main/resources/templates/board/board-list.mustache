<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <title>게시글 목록</title>
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            min-height: 100vh;
        }

        .container {
            width: 700px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
            overflow: hidden;
        }

        .tab-header {
            display: flex;
            background-color: #007bff;
        }

        .tab-header a {
            flex: 1;
            text-align: center;
            padding: 15px 0;
            color: white;
            text-decoration: none;
            font-weight: bold;
            border-bottom: 3px solid transparent;
            transition: background-color 0.3s, border-bottom 0.3s;
        }

        .tab-header a:hover {
            background-color: #0056b3;
            border-bottom: 3px solid white;
        }

        .list-container {
            padding: 30px 40px;
        }

        .list-container h2 {
            margin-bottom: 20px;
            color: #333;
        }

        .post-item {
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 1px solid #ddd;
        }

        .post-item a {
            text-decoration: none;
            color: #007bff;
            font-weight: bold;
        }

        .post-item a:hover {
            text-decoration: underline;
        }

        .post-meta {
            font-size: 16px;
            margin-bottom: 5px;
        }

        .post-content {
            font-size: 14px;
            color: #555;
            white-space: pre-line;
        }

    </style>
</head>
<body>

<div class="container">
    <!-- 상단 탭 -->
    <div class="tab-header">
        <a href="/">메인 페이지</a>
        <a href="/board/save-form">게시글 작성</a>
    </div>

    <!-- 게시글 리스트 -->
    <div class="list-container">
        <h2>게시글 목록</h2>

        <!-- 게시글 항목 반복 -->
        {{#boardList}}
        <div class="post-item">
            <div class="post-meta">
                <a href="/board/{{boardIdx}}">
                    게시글 번호: {{boardIdx}}, 게시글 제목: {{boardTitle}}
                </a>
            </div>
            <div class="post-content">
                {{boardContent}}
            </div>
        </div>
        {{/boardList}}

        <!-- 게시글이 없을 때 -->
        {{^boardList}}
        <p>등록된 게시글이 없습니다.</p>
        {{/boardList}}

    </div>
</div>

</body>
</html>
