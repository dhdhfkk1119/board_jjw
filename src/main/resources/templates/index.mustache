<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8"/>
    <title>JJW Login</title>
    <style>
        * {
          box-sizing: border-box;
        }

        body {
          margin: 0;
          padding: 0;
          height: 100vh;
          font-family: 'Segoe UI', sans-serif;
          background-color: #f0f0f0;
          display: flex;
          justify-content: center;
          align-items: center;
        }

        .container {
          display: flex;
          flex-direction: column;
          align-items: center;
          width: 520px;
        }

        /* 상단 바 */
        .tab-header {
          width: 100%;
          display: flex;
          border-radius: 10px 10px 0 0;
          overflow: hidden;
        }

        .tab-header a {
          flex: 1;
          text-align: center;
          padding: 15px 0;
          background-color: #007BFF;
          color: white;
          text-decoration: none;
          font-weight: bold;
          transition: background-color 0.3s;
        }

        .tab-header a:hover {
          background-color: #0056b3;
        }

        /* 로그인 박스 */
        .login-box {
          width: 100%;
          background-color: #ffffff;
          border-radius: 0 0 10px 10px;
          box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
          padding: 30px 30px 40px;
          box-sizing: border-box;
          color: #333;
        }

        /* 회원 정보 뷰 */
        .login-box.flex-row {
          display: flex;
          gap: 20px;
          align-items: flex-start;
        }

        .profile-image {
          border-radius: 10px;
          width: 150px;
          height: 150px;
          object-fit: cover;
        }

        .member-info {
          flex: 1;
          display: flex;
          flex-direction: column;
          justify-content: flex-start;
        }

        .member-info .bold {
          font-weight: bold;
          font-size: 20px;
          margin-bottom: 10px;
        }

        .member-info .small {
          font-size: 14px;
          color: #555;
          margin-bottom: 20px;
        }

        .member-info .right-align {
          margin-top: auto;
          text-align: right;
        }

        .update-form {
          display: inline-block;
          padding: 8px 16px;
          background-color: #007BFF;
          color: white;
          border-radius: 5px;
          text-decoration: none;
          font-weight: bold;
          transition: background-color 0.3s;
        }

        .update-form:hover {
          background-color: #0056b3;
        }

        /* 로그아웃 버튼 영역 */
        .logout-container {
          margin-top: 30px;
        }

        .logout-box {
            border-top: none;
            border-radius: 0 0 10px 10px;
            padding: 0; /* 안쪽 여백 제거 */
            margin-top: -10px; /* 위 login-box와 연결되도록 (선택적) */
        }

        .logout-btn {
            width: 100%;
            padding: 20px 0;
            border: none;
            border-radius: 0 0 10px 10px;
            font-size: 18px;
            font-weight: bold;
            color: white;
            cursor: pointer;
            background-color: #dc3545;
            transition: background-color 0.3s;
            display: block;
        }

        .logout-btn:hover {
          background-color: #a52834;
        }

        /* 로그인 폼 */
        form h2 {
          margin-bottom: 30px;
          color: #333;
          text-align: center;
        }

        .input-group {
          display: flex;
          flex-direction: column;
          align-items: flex-start;
          margin-bottom: 20px;
          width: 100%;
        }

        .input-group label {
          margin-bottom: 5px;
          font-weight: bold;
          font-size: 14px;
          color: #555;
        }

        .input-group input {
          width: 100%;
          padding: 10px;
          font-size: 14px;
          border: 1px solid #ccc;
          border-radius: 5px;
        }

        .button-group {
          margin-top: 20px;
          display: flex;
          justify-content: center;
        }

        .button-group button {
          width: 100%;
          padding: 10px 0;
          background-color: #007BFF;
          border: none;
          border-radius: 5px;
          color: white;
          font-weight: bold;
          font-size: 16px;
          cursor: pointer;
          transition: background-color 0.3s;
        }

        .button-group button:hover {
          background-color: #0056b3;
        }

        /* 에러 메시지 */
        .error-message {
          color: red;
          font-size: 13px;
          margin-top: 5px;
          width: 100%;
        }
    </style>
</head>
<body>

<div class="container">

    <!-- 로그인 상태 -->
    {{#sessionUser}}

        <!-- 상단 바 -->
        <div class="tab-header">
            <a href="/board/save-form">게시글 작성</a>
            <a href="/board/list">게시글 리스트</a>
        </div>

        <!-- 로그인 후 회원 정보 뷰 -->
        <div class="login-box flex-row">

            <div>
                <img src="/local-profile/{{sessionUser.profileImage}}" alt="프로필 이미지" class="profile-image"/>
            </div>

            <div class="member-info">
                <div class="bold">회원 번호 : {{sessionUser.Id}}</div>
                <div class="bold">회원 이름 : {{sessionUser.memberName}}</div>
                <div class="small">회원 아이디 : {{sessionUser.memberId}}</div>
                <div class="right-align">
                    <a href="/member/update-form" class="update-form">회원 정보 수정</a>
                </div>
            </div>

        </div>

        <!-- 로그아웃 버튼 -->
        <div class="login-box logout-box">
            <form action="/member/logout" method="post">
                <button type="submit" class="logout-btn">로그아웃</button>
            </form>
        </div>

    {{/sessionUser}}

    <!-- 비로그인 상태 -->
    {{^sessionUser}}

        <div class="tab-header">
            <a href="/">로그인</a>
            <a href="/member/save-form">회원가입</a>
        </div>

        <div class="login-box">
            <form action="/member/login" method="post">
                <h2>JJW-Login</h2>

                <div class="input-group">
                    <label for="username">아이디</label>
                    <input type="text" id="username" name="memberId" placeholder="아이디를 입력하세요"/>
                    {{#errors.memberId}}
                        <p class="error-message">{{errors.memberId}}</p>
                    {{/errors.memberId}}
                </div>

                <div class="input-group">
                    <label for="password">비밀번호</label>
                    <input type="password" id="password" name="password" placeholder="비밀번호를 입력하세요"/>
                    {{#errors.password}}
                        <p class="error-message">{{errors.password}}</p>
                    {{/errors.password}}
                </div>

                <div class="button-group">
                    <button type="submit">로그인</button>
                </div>

                {{#isErrors}}
                    <p class="error-message">{{isErrors}}</p>
                {{/isErrors}}
            </form>
        </div>

    {{/sessionUser}}
</div>

</body>
</html>
