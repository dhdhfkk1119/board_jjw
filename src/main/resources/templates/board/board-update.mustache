<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <title>게시글 작성</title>
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
            display: flex;
            justify-content: center;
            align-items: flex-start;
            min-height: 100vh;
        }

        /* 전체 박스 */
        .container {
            width: 700px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
            overflow: hidden;
        }

        /* 탭 헤더 */
        .tab-header {
            display: flex;
            background-color: #007bff;
        }

        .tab-header a {
            flex: 1;
            text-align: center;
            padding: 15px 0;
            color: white;
            text-decoration: none;
            font-weight: bold;
            border-bottom: 3px solid transparent;
            transition: background-color 0.3s, border-bottom 0.3s;
        }

        .tab-header a:hover {
            background-color: #0056b3;
            border-bottom: 3px solid white;
        }

        /* 폼 영역 */
        .form-container {
            padding: 30px 40px;
        }

        .form-container h2 {
            margin-bottom: 20px;
            color: #333;
        }

        .title-row {
            display: flex;
            align-items: center;
            margin-top: 10px;
            margin-bottom: 20px;
        }

        label {
            font-weight: bold;
            margin-right: 10px;
            white-space: nowrap;
        }

        input[type="text"] {
            flex: 1;
            padding: 8px;
            font-size: 1rem;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        textarea {
            width: 100%;
            min-height: 150px;
            padding: 10px;
            font-size: 1rem;
            border-radius: 4px;
            border: 1px solid #ccc;
            resize: vertical;
        }

        .btn-submit {
            margin-top: 30px;
            background-color: #007bff;
            border: none;
            color: white;
            padding: 10px 18px;
            font-size: 1rem;
            font-weight: bold;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .btn-submit:hover {
            background-color: #0056b3;
        }

    </style>
</head>
<body>

<div class="container">
    <!-- 상단 탭 -->
    <div class="tab-header">
        <a href="/board/list">게시글 목록</a>
        <a href="/board/save-form">게시글 작성</a>
    </div>

    <!-- 폼 내용 -->
    {{#board}}
    <div class="form-container">
        <h2>게시글 작성</h2>
        <form action="/board/update/{{boardIdx}}" method="post">
            <div class="title-row">
                <label for="title">게시글 제목 :</label>
                <input type="text" id="title" name="boardTitle" placeholder="제목을 입력하세요" value="{{boardTitle}}" />
            </div>

            <label for="content">게시글 내용 :</label>
            <textarea id="content" name="boardContent" placeholder="내용을 입력하세요">
            {{boardContent}}
            </textarea>

            <button type="submit" class="btn-submit">게시글 작성 완료</button>
        </form>
    </div>
    {{/board}}
</div>

</body>
</html>
