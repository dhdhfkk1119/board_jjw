<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <title>게시글 상세보기</title>
    <style>
        * {
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            min-height: 100vh;
        }

        .container {
            width: 700px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            margin-top: 50px;
            overflow: hidden;
        }

        .tab-header {
            display: flex;
            background-color: #007bff;
        }

        .tab-header a {
            flex: 1;
            text-align: center;
            padding: 15px 0;
            color: white;
            text-decoration: none;
            font-weight: bold;
            border-bottom: 3px solid transparent;
            transition: background-color 0.3s, border-bottom 0.3s;
        }

        .tab-header a:hover {
            background-color: #0056b3;
            border-bottom: 3px solid white;
        }

        .detail-container {
            padding: 30px 40px;
        }

        .detail-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .detail-header h2 {
            margin: 0;
            font-size: 24px;
            color: #333;
        }

        .button-group {
            display: flex;
            gap: 10px;
        }

        .button-group a {
            padding: 8px 16px;
            background-color: #6c757d;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            font-size: 14px;
            font-weight: bold;
            transition: background-color 0.3s;
        }

        .button-group a:hover {
            background-color: #5a6268;
        }

        .btn-edit, .btn-delete {
            padding: 8px 14px;
            border-radius: 5px;
            font-weight: bold;
            font-size: 14px;
            text-decoration: none;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        /* 수정 버튼 스타일 */
        .btn-edit {
            background-color: #28a745;
            color: white;
            border: none;
        }

        .btn-edit:hover {
            background-color: #218838;
        }

        /* 삭제 버튼 스타일 */
        .btn-delete {
            background-color: #dc3545;
            color: white;
            border: none;
        }

        .btn-delete:hover {
            background-color: #c82333;
        }


        .detail-item {
            margin-bottom: 20px;
        }

        .label {
            font-weight: bold;
            color: #555;
            margin-bottom: 5px;
            display: block;
        }

        .value {
            font-size: 16px;
            color: #333;
            white-space: pre-line;
        }
    </style>
</head>
<body>

<div class="container">

    <!-- 상단 탭 -->
    <div class="tab-header">
        <a href="/">메인으로</a>
        <a href="/board/list">게시글 목록</a>
    </div>

    <!-- 게시글 상세 내용 -->
    {{#board}}
    <div class="detail-container">
        <!-- 제목 및 버튼 -->
        <div class="detail-header">
            <h2>게시글 상세보기</h2>
            {{#isOwner}}
            <div class="button-group">
                <a href="/board/update-form/{{boardIdx}}" class="btn-edit">수정</a>
                <form action="/board/delete/{{boardIdx}}" method="post" onsubmit="return confirm('정말 삭제하시겠습니까?');">
                    <button type="submit" class="btn-delete">삭제</button>
                </form>
            </div>
            {{/isOwner}}
        </div>

        <div class="detail-item">
            <span class="label">제목</span>
            <div class="value">{{boardTitle}}</div>
        </div>

        <div class="detail-item">
            <span class="label">내용</span>
            <div class="value">{{boardContent}}</div>
        </div>

        <div class="detail-item">
            <span class="label">작성자</span>
            <div class="value">{{writerName}}</div>
        </div>

        <div class="detail-item">
            <span class="label">작성일</span>
            <div class="value">{{createAt}}</div>
        </div>
    </div>
    {{/board}}

</div>

</body>
</html>
